<?xml version="1.0"?> 

<node name="clampC" author="mesh">
	<help>
	clamp(input,min,max) returns "min:  if "input" is less than "min", "max" if a is greater than "max"; 
	otherwise it returns "input"
	</help>
	<input>
	  <property name="input" type="color" default="color(0,0,0)"/>	
		<property name="min" type="color" default="color(0,0,0)"/>		
		<property name="max" type="color" default="color(1,1,1)"/>		
	</input>
	<output>
		<property name="result" type="color"/>		
	</output>
	<code>
<![CDATA[
 $(result) = clamp(($(input)), ($(min)), ($(max)));
]]>
	</code>		
</node>
